import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  textarea: {
    borderWidth: 1,
    borderColor: "#888",
    borderRadius: 8,
    minHeight: 72,
    maxHeight: 144,
    paddingHorizontal: 16,
    paddingTop: 16,
    paddingBottom: 16,
    color: "#333",
  },
  textareaFocused: {
    borderColor: "#222",
  },
});
